import "./Pattern.css"; // Import the CSS file for styling

const Pattern = () => {
  return (
    <div className="container"></div>
  );
};

export default Pattern; // Export the component for use in other files
